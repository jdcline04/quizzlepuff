import React from 'react'

const TitleBar = () => {
    const title = "Welcome to One Quiz";
    return (
        <div className="main-title-bar">
            <h1 className="main-title-text">{title}</h1>
        </div>
    )
}

export default TitleBar;